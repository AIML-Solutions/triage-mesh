apiVersion: v1
kind: Secret
metadata:
  name: mesh-auth
type: Opaque
stringData:
  MESH_SECRET: {{ .Values.meshSecret | quote }}
  {{- if .Values.anthropicApiKey }}
  ANTHROPIC_API_KEY: {{ .Values.anthropicApiKey | quote }}
  {{- end }}
