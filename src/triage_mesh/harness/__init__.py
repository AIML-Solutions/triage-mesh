"""Shared agent harness: model providers, structured logging, A2A serving.

Agents differ only in card, toolset, and handler; everything cross-cutting
lives here so phase 2's policy engine and trust labels land in one place.
"""
